{
  "name": "@craft-agent/pi-agent-server",
  "version": "1.1.3",
  "license": "Apache-2.0",
  "description": "Out-of-process Pi agent server communicating via JSONL over stdio",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pi-agent-server": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=bun --format=esm --external koffi && bun build src/tools/read/image-resize-worker.ts --outfile=dist/image-resize-worker.js --target=bun --format=esm && bun run scripts/copy-runtime-assets.ts",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "validate": "bun run typecheck && bun run build"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.81.1",
    "@earendil-works/pi-agent-core": "0.81.1",
    "@earendil-works/pi-ai": "0.81.1",
    "@silvia-odwyer/photon-node": "0.3.4",
    "@sinclair/typebox": "^0.34.0",
    "node-html-parser": "^6.1.0",
    "pdfjs-dist": "^5.4.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
