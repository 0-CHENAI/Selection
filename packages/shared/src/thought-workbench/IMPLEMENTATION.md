# Selection 编排工作台实施状态

基线：`test` / `097303e64c5ab0b73a378220fe3f07dd986ffd42`。
隔离分支：`codex/thought-workbench`。此文件记录实施状态，不代表整个替换计划已完成。

## 已落地的第一批代码

最新输入快照验收（覆盖下文较早的“Agent 快照尚未接入”记录）：Agent 的完整平台提示、消息、图片和注册工具现在通过同一组装路径预览，工作台生成使用对应会话的一次性哈希；运行时模型/工具或输入变化拒绝发送。预览租约过期只清理未使用的空草稿，不删除后续会话。已有界面展示完整文本、工具、模型与哈希，图片以原件摘要表示，不重复铺开 Base64。相关 RPC、并发取消、旧预览拒绝、单次使用和会话保留回归通过。

实际传输验证：完整 PiAgent（只替换凭据获取，避免读取用户密钥）启动真实打包子进程，使用本地 HTTP 模型端点。预览阶段零模型请求；发送系统提示、用户内容及工具列表与快照一致；通过真实 `submit_answer` 工具路径完成一次正式答案提交，并取得 SDK 消息标识。该测试未使用真实模型，不证明回答质量、Token 收益、审批 UI、完整 SessionManager/桌面端端到端链路或发布验收。默认入口开关仍未切换；任务尚未整体完成。

- 固定 ThoughtDAG 来源提交 `ef04210f6106a0dbc30f353cf67b25bee47d769c`；上下文遍历适配为 Selection 模块，MIT 声明见同目录 `THIRD_PARTY.md`。
- 文件系统工作台文档、严格版本模式、原子写入、修订号 CAS、防路径穿越与符号链接检查。
- 独立思考节点、上下文／引用边、上下文编译、内容哈希、版本过期判定函数、持久生成记录。
- 无工具问答使用独立临时会话的 `queryLlm`；Agent 使用独立会话并订阅完成事件；取消后拒绝迟到结果。
- 默认关闭的 `CRAFT_FEATURE_THOUGHT_WORKBENCH` 开关。置为 `1` 构建后，原创建入口默认进入思考画布，编辑入口默认进入执行画布。
- 思考画布的分支／多选合并、连线、节点编辑、搜索、版本选择、上下文预览、取消、草稿自动保存和 JSON 导出。
- 执行画布增删节点、布局保存、依赖增删、环校验、删除影响确认和完整 JSON 配置编辑；沿用原表单与 YAML 的字段保留和保存验证机制。
- 选定思考内容可传入现有编排提案组件；任务明确保存后记录工作台关联。没有更改 TaskRunner、审批与运行历史。
- 提案历史现独立持久保存，记录修改意图、完整执行基线、选定思考上下文、结果和应用／放弃状态。新一轮生成传入此前历史；应用只改工作台执行草稿。
- 服务端在生成和应用时验证图版本、执行草稿哈希及真实任务 ETag；应用中断恢复标记和幂等重试已覆盖。客户端不能伪造恢复标记。
- 面板接入历史审阅、按节点 ID 的完整字段差异、服务端应用／放弃，以及本地编辑失效检查。此批 UI 尚待交互验收。
- 桌面会话菜单可选已有／新建工作台导入已完成消息，服务端隔离工作区、排除流式预览与非正式答案，重复导入不恢复主动剪断的上下文边。
- 运行结果面板支持将真实节点输出导入思考视图。按任务、run、节点执行 revision、节点及 attempt 去重；来源内容只读，原始来源记录允许撤销图删除而不允许篡改。
- 材料原件按 SHA-256 保存为工作台本地二进制文件，读取校验摘要。接入 PDF 分页提取／画布阅读、DOCX 原文提取、HTML 无网络纯解析、文本／代码和图片阅读；选区记录页码与字符偏移且不自动带入整份材料。
- 问答接口支持显式有序 user／assistant 历史、独立 system 指令和图片字节；严格输入模式使用内存设置、禁用工具和自动压缩，不静默换模型或扩展模板。Agent 图片走既有附件接口。
- 引用深度与合并顺序可在侧面板编辑；重复引用合并最深显式范围并去重。继承材料可以在后继节点排除，显示当前答案上下文过期状态。图撤销保留新增答案版本及任务关联，已执行的新节点撤销创建时归档而非抹除记录。
- 显式重放目标按依赖排序，不自动加入未选中的 Agent 祖先；服务端串行调度、持久队列记录、进度／取消 UI 已接通。每步使用前一步交付后的最新上下文，失败即停止，重启或导入后标记中断不自动重跑。尚待 Agent 审批与断线真实验收。
- 完整本地备份服务与导入／导出按钮接入：包含文档、原始材料、提案对话、来源记录及生成快照。校验编码、摘要、路径、身份及原件完整性后暂存并原子发布。导入为独立草稿，解除任务关联、重编号生成记录并将运行中记录标记中断，不自动执行。
- Markdown 报告与自包含 SVG 图像导出已接入；报告保留答案版本、来源、材料文本及关系，SVG 保留画布布局与引用边并转义用户文本。尚待实际导出文件视觉验收。
- 手动答案修订经服务端 CAS 校验保存为独立 manual 版本，不调用模型或覆盖原答案版本；侧面板支持文本选区高亮、标记／过滤／关闭三种上下文模式，含下游输入回归测试。
- 草稿切换与备份导入现在切换整个编辑目标，切换前保留当前执行草稿，重新挂载思考／执行视图；已保存任务通过 ETag 检查后恢复草稿，冲突时阻止静默恢复。尚待多草稿真实 UI 回归及冲突解决交互。
- 取消边界回归：重放／生成中断记录写盘失败时仍执行底层取消，生成启动记录写盘失败时清理未派发会话。相关重放失败与取消单元测试已通过。
- 自动分层布局与选中节点上游路径定位已接入：覆盖合并及引用边，非路径内容淡化，布局操作不改变上下文哈希。尚待大图真实画布与键盘交互验收。
- 紧凑会话菜单已接入工作台导入，使用抽屉内列表而非易被裁切的嵌套菜单；与桌面菜单共享导入逻辑，按需加载草稿。窄屏真实交互仍待验收。
- 浏览器真实验收已开始：隔离服务器／工作区，顶部创建进入思考视图，草稿自动保存、手动修订、下游上下文预览通过实际操作。发现并修复动态环境变量导致浏览器构建开关失效的问题，新增无 process.env 的构建回归测试；思考画布控件接入明暗主题。尚未完成真实模型或桌面安装包验收。
- 真实浏览器备份往返通过：导入文本原件后下载完整备份，再上传为独立副本；原文档和副本各保留 3 个节点、1 个答案版本，原件逐字节一致且未创建任务。修复导入材料后旧节点预览残留，预览现在匹配文档／节点／修订号；完整导出前同步执行草稿。
- 思考分组支持命名、折叠／展开、解除及整体移动；成员唯一性校验、撤销和完整备份保留分组。分组操作不改变上下文哈希，拖动复用分组边界避免每个成员重复计算。真实拖动与分组导出视觉验收尚待完成。
- 隔离浏览器已实际验证分组创建、命名、折叠及持久化；SVG 支持分组边框和折叠状态，Markdown 保留完整成员内容及分组元数据，补充分组导出测试。
- 真实拖动发现并修复分组负层级和尺寸未声明导致的画布平移问题。修复后浏览器拖动折叠分组，成员节点位置从 (0, 0) 写入为 (45, 27.5)，而非仅改变视口。
- 思考节点模型／连接与角色指令编辑已接入，模型选项使用连接及模型的复合标识，同名模型不会在思考节点选择时被最后一个连接覆盖；模型目录补充连接身份回归测试。
- 摘要操作创建独立无工具问答节点，按选中顺序保留完整引用，原分支与答案不改写，生成仍需显式预览和确认。引用结果节点时补充来源元数据，包含摘要来源回归测试；不宣称摘要自动降低后继上下文 Token。

## 尚未达到的验收项（功能开关不得默认启用）

保存／运行补充：统一保存按钮已覆盖思考视图，纯思考草稿保存不创建任务；新工作台新增显式创建并运行，首次运行失败后退出创建态而保留已创建任务，避免重试重复创建。保存从思考视图发起时仍使用未被表单修改取代的 YAML 原稿。首次运行生命周期 3 项单元测试通过，真实 UI 与运行服务闭环仍待验收。

生成并发补充：服务端答案提交在异步哈希期间遇到图修订变化时重新读取并比较，不再把这类正常编辑竞态直接记作生成失败。旧输入答案保留为非活动版本，取消后不写入；并发编辑和哈希期取消单元测试已通过。客户端编辑锁尚未解除，需先实现本地未保存编辑与服务端版本合并。

三方合并基础已实现：按字段和节点身份合并独立编辑，拒绝同字段与删除／编辑冲突，按上游顺序检查新增活动答案的输入哈希。保存确认晚于继续输入时已使用该合并；3 项测试覆盖输入编辑、布局不致过期、答案历史、冲突与独立增删。生成完成刷新、保存 CAS 冲突恢复和可操作冲突面板尚未接通，不能据此宣称生成中编辑已可用。

后续进展：保存 CAS 冲突恢复、生成完成与重放刷新已接入三方合并；轮询防重叠、拒绝旧修订覆盖，哈希期间继续输入会重新合并。单次生成期间已开放标题、问题与角色指令编辑及自动保存；其余编辑、重放期间编辑及跨图操作仍有限制。合并／保存 8 项测试通过，生成期真实交互和冲突解决面板仍待验收，前述“尚未接通”记录由本条更新。

服务端闭环补充：RPC 测试验证生成中保存问题修改不会改变已派发输入，旧输入答案仅入历史；布局修改不使答案过期；旧客户端保存可合并新交付版本。普通保存拒绝修改／删除已有版本、重复版本 ID 和活动答案正文不匹配，但允许切换历史版本。当前 RPC 14 项测试通过；使用受控模型替身，不是实际模型网络或浏览器端到端证明。

恢复发现接口已接入：按工作台枚举生成记录，失去内存运行实例的 running 记录持久标为 interrupted，仍在运行者不变。打开已保存草稿可重新附着运行中生成的轮询，不发起模型或工具调用。RPC 16 项测试通过；多生成并行展示、中断记录界面、重放恢复发现与真实重启验收仍待完成。

分发许可已接线：macOS、Windows、Linux 的 extraResources 均从固定来源声明复制到 third-party/ThoughtDAG-NOTICE.txt。配置测试验证三平台源路径、提交号、MIT 授权和免责声明；尚未以实际安装包解包验证，不代表打包验收完成。

导出一致性修复：Markdown／SVG 与完整备份一样先同步执行草稿并保存，再导出固定快照，不再使用可能落后的渲染状态。SVG 替换 XML 1.0 禁止的控制字符与孤立代理字符，保留中文和 emoji；4 项导出测试通过，真实下载与视觉验收仍待完成。

材料失败清理：目标节点校验移到原件写入之前；文档提交失败只回收本次新创建的原件，不删除已有摘要文件。空目标、未知目标、模拟磁盘错误和共享原件保留测试通过；材料测试现 6 项，复杂格式真实阅读与内存验收仍待完成。

PDF 阅读器资源优化：同一原件跨页复用 PDF 文档，切页只取消／替换页面渲染，完成后清理页面资源，关闭或更换原件时销毁加载任务。原件读取不再依赖每次重建的页数组，避免图更新导致重复解码；类型和 lint 通过，真实切页与内存测试尚待完成。

HTML 提取使用无 DOM 的迭代遍历，避免深层嵌套递归栈溢出；排除脚本、样式、模板及嵌入文档，表格单元格使用制表符分隔。文本顺序及 12,000 层嵌套输入测试通过，不代表全部材料格式验收完成。

近期整合验证：开启工作台开关的 renderer 生产构建发现 merge/save/answer 未加入共享包 exports，已修复并新增导出清单测试。重跑构建成功（22.72 秒，仍有大 chunk 警告）；核心工作台／RPC／重放／执行图及入口测试 96 项通过，材料与历史面板另 9 项通过，许可和模块导出检查 2 项通过。此为构建与回归证据，不替代真实模型、安装包及全计划验收。

桌面构建补充：主进程和 bootstrap preload 打包通过；未读取 OAuth .env。发现 build:validate 引用不存在脚本，补上构建产物及核心复制资源的内容校验器，缺失／过期资源测试通过。资源复制已完成。

macOS ARM64 实包验收：最新主进程、Pi 子进程和启用工作台的 renderer 构建通过；`electron-builder --mac --arm64 --dir --publish never` 成功生成 `apps/electron/release/mac-arm64/Selection.app`，仅本地 ad-hoc 签名，未公证或发布。包内 ThoughtDAG 许可、主进程、Pi bundle、Photon WASM 与源产物 SHA-256 一致；Bun、ripgrep、renderer 入口存在。包内 Bun 实际输出 1.3.9，Pi 子进程完成启动及 stdin EOF 正常退出（无模型调用）。这不证明图形界面、真实模型、DMG 安装或其他平台验收完成。打包器仍有 ASAR 关闭及依赖收集警告；Koffi 复制警告对应当前 bundle 无 Koffi 导入，未阻止本次启动。

1. 真实模型的精确请求快照与窗口校验：问答的有序角色历史、系统指令和原图字节已通过打包 Pi 子进程至本地 HTTP 模型端点的实际请求验证，剪断唯一标记不再发送，取消不提交部分答案，超窗不发出请求。工作台 Agent 也已接通 strictInput：包含实际系统提示、历史、图片及工具定义的窗口检查，禁用自动压缩及 Pi 模板扩展；打包子进程的 Agent 超窗零网络请求测试通过。Agent 完整输入快照已接入工作台，完整 PiAgent／打包子进程验证见本文顶部；桌面端端到端和真实模型验收仍待完成。没有完成真实模型质量与 Token 收益比较。
2. Agent 答案现按完成事件的消息 ID 读取并检查正式交付状态，RPC 已覆盖拒绝预览／未提交答案。问答生成期间允许修改问题、标题和角色；取消、跨图隔离、历史记录发现已有回归。完整流式事件序号、真实审批与断线恢复仍待验收；没有自动恢复／重跑工具。
3. 材料导入、原件管理、PDF 页码选区、继承材料排除、完整本地备份及 Markdown／SVG 导出 UI 已接入，尚待真实阅读交互、导出视觉检查与大文件内存验收。PNG／JPEG／WebP／GIF 已接入模型输入，实际视觉模型验收尚未完成。
4. 摘要、分组、自动布局、路径定位、按依赖重放、手动修订、高亮、桌面／紧凑会话添加和结果去重回流已接入。会话附件复制原件并固定来源，文本／图片／HTML／PDF／DOCX 支持本地读取或提取，PDF 保留逐页文本、DOCX 保留脚注尾注，拒绝跨会话路径和无效文件；附件随备份恢复，来源材料不可通过普通保存篡改。完整真实交互及大材料内存验收仍待完成。
5. 提案保存新增／修改执行节点与选定思考上下文的历史关联，已应用关联可通过 executionSources 查询。来源展示、字段差异定位，以及真实模型的三轮修改闭环仍待完成。持久提案对话历史、字段级差异和服务端基线绑定已接入，不能用单元测试代替 UI 与模型验收。
6. 执行配置专用侧面板、统一顶栏、草稿执行定义恢复和冲突解决：全局配置控件已复用到执行画布内可折叠的高级面板，新工作台移除旧定义页选项；旧表单仅保留功能开关关闭的回退入口。节点高级字段仍可通过 JSON 和 YAML 完整编辑，专用配置与冲突解决的完整交互验收仍待完成。
7. 跨文档／TaskSpec 的保存与冲突恢复：首次保存和已有任务更新均在 TaskSpec 原子写入前记录精确目标 ETag 和工作台基线；重开工作台可恢复关联，父会话创建失败保留已保存任务并明确提示，不重建任务或自动运行。任务写入前失败允许保留旧关联并重试；过期 ETag 在改写日志前拒绝。仍需冲突恢复的完整 UI 验收；不能将当前两文件恢复协议称为通用事务。
8. 100／500／1,000 节点链式／合并图的编译、布局、输入哈希不变及重放拓扑测试已通过（scale.test.ts）。隔离浏览器链式画布验收发现尺寸通知反复重建受控节点，导致节点隐藏、连线不显示；已固定节点尺寸并避免尺寸通知改写选择状态。修复后实际 DOM 分别显示 100/99、500/499、1000/999 个节点/连线，无隐藏节点及错误提示。1,000 节点搜索 `Scale node 999` 只保留目标节点，自动化输入调用耗时 256ms（含工具开销，不是精确渲染基准）。缩放下限支持全图概览。流式更新、复杂合并图 UI、材料内存及精确帧耗时测试仍待完成。
9. 真实桌面安装包、主题、窄窗口、键盘、无障碍、V1/V2 明确迁移及运行审批端到端验收，分发许可清单接线。
10. 完成后同步 #354 与 #320 验收记录，再切换默认入口、移除旧组件。当前未更新远程 Issue。

## 已执行验证

- 执行边界集中审查：编排 UI、任务导入／提案、TaskRunner 与结果投影共 25 文件、241 项通过。补充遍历权威 `NODE_KINDS` 的 V3 标题编辑往返回归，验证编辑器类型清单不缺项，并保留模型连接、权限、输入输出、条件、循环、重试、路由等高级字段；该文件 37 项通过。这是字段保真与执行服务回归，不是每类节点的真实模型运行验收。

- 最新输入链路审查回归：共享工作台、RPC、重放、会话查询生命周期、问答输入与客户端协议共 23 文件、146 项通过。另修复查询派发同步抛错时的关联记录残留，客户端 10 项回归通过。此项仅证明这些测试覆盖的协议／存储边界，不替代尚未完成的发布验收。

- 普通问答实际 UI 验收：新版隔离服务上，`legacy-model-qa` 预览展示解析后的模型、完整消息、空工具表及 SHA-256，模型端点累计请求保持 1；明确生成后变为 2，原始系统及用户内容一致、无工具，答案版本 `946a7421-8b39-428e-90e6-01345c42407b` 保存且未创建任务。再次预览后将隔离连接窗口从 262144 改为 131072，发送显示 `Query input preview changed`，请求数仍为 2；随后恢复测试配置。此为真实 UI／服务／子进程与本地确定性端点验证，不代表真实模型质量。

- 普通问答快照：与窗口检查共用的 `snapshotExplicitQuery` 已接通工作台预览、一次性租约和发送前校验；预览返回完整解析模型及无工具输入，不调用模型。哈希覆盖有序消息、原图字节及运行配置，模型会话准备后再次校验，变化则拒绝发送。复用现有快照展示和重放传递路径。子进程真实本地 HTTP 回归验证预览零请求、原输入成功及变化输入零请求；相关合并回归 44 项通过。修复相同输入哈希的跨草稿缓存覆盖后 RPC 33 项通过。尚待更新隔离服务后的实际问答 UI 验收；本地端点不代表真实模型质量验收。

- 最新 macOS ARM64 应用目录包重新构建完成，逐字节核对打包后的主进程、Pi 子进程资源及 `third-party/ThoughtDAG-NOTICE.txt` 与本次源码一致，`codesign --verify --deep --strict` 通过。使用 ad-hoc 签名，未公证或发布；这不替代安装后内嵌服务及真实模型验收。

- 收尾批次：全仓 `typecheck:all` 与启用工作台的完整 `electron:build` 通过。22 文件合并回归 146 项中最初 145 通过；唯一失败是重试投影修复后拒绝导入旧结果的错误文案断言，更新为当前提前拒绝的原因后该文件 6 项全部通过。未重复运行已通过的其他文件。旧草稿未绑定模型的实际 UI 验收确认：点击预览后修订号由 1 升到 2，节点明确保存界面显示的模型及连接，已有选择不变；预览不调用模型。

- 2026-09-14 更新后的真实 SessionManager／浏览器链路：隔离草稿 `2cbef971-471f-4610-99e5-f613d25632d4`，Agent 预览展示完整模型、平台提示、消息及 52 个工具定义，本地模型端点在预览时请求数为 0；明确生成后请求数为 1，保留 `QA_LITERAL` 引用标签。生成 `a955c307-8e71-4c0b-9877-42482f6309e8` 完成，答案版本写入工作台；会话 `260914-dynamic-coast` 的对应消息为 `answerCommitted: true` / `explicit-v1`。没有创建 TaskSpec 或运行 DAG。另修复长问题溢出节点以及纯思考草稿标题引起的假未保存状态，真实界面验证三行限制与重命名自动保存后的 Saved。此轮使用无凭据的本地确定性端点，不计作真实模型质量验收。

- 共享大材料规模回归：100／500／1,000 节点分别引用相同的 1,150,000 字符材料，编译结果均只保留一份；改用摘要／页码／文本分层索引，避免每次继承引用都 JSON 序列化全文。一次本地 Bun 运行编译耗时约 11.6／9.8／10.2 ms，观测 heapUsed 增量约 5.3／5.8／6.2 MB；RSS 受运行时分配和 GC 影响，仅作为诊断输出。文本提取版本、页码、不同原件判重语义及输入哈希回归通过。不代表 PDF 解码峰值、持续泄漏测试或浏览器帧率验收。

- 项目一致性补充：上下文快照与哈希包含项目 ID；高级面板项目选择、提案应用、YAML 草稿备份和任务关联恢复同步项目元数据，格式错误的 YAML 保留原元数据而不丢弃草稿。相关上下文、提案、恢复与元数据定向回归通过。此项不等于项目文件内容、平台指令、工具定义和解析后模型配置已全部进入 Agent 输入快照。
- 隔离服务重启后重新打开已停止运行，状态仍为 stopped，已完成审批节点不再显示旧拒绝 blocker，也未自动恢复执行。另修复验证会话通过完成事件报告 error/timeout、但 sendMessage 正常返回时运行悬停 verifying 的问题；V2/V3 定向回归覆盖错误收敛、历史读取及普通文本不能替代正式 verdict。该错误收敛修复尚待新版隔离服务实际验证。

- 隔离浏览器 `panel-qa` 实际运行验收：保存与重命名后仅存在任务定义；明确点击 Save & Run 后创建 `run-1789319648822`，依次完成反馈仍等待、拒绝失败、重试重新等待、批准节点完成、验证阶段暂停／恢复及停止。运行日志序号 5–20 记录相应事件，最终 stopped，tokensUsed/modelMs 均为 0；不证明真实模型验证或工具型节点重试。发现旧失败原因被当作已恢复节点的当前 blocker，已修复快照投影，保留失败历史用于重试提示；TaskRunner 96 项测试通过，包括进程恢复前后审批重试的 blocker 回归。修复后的服务尚待重新启动做 UI 回归。

- 工作台入口与编辑一致性补充：模板、导入和迁移提示不再依赖旧定义页；思考上下文提案进入执行画布并展开提案面板。提案输入及失效身份共用当前 YAML／表单定义；切换画布前等待 YAML 校验，忽略过期校验回执。顶栏重命名保留 YAML 高级字段、注释和别名，提案应用、保存备份及关联恢复同步标题。相关定向测试与全仓类型检查通过；新高级面板的实际交互与竞态测试仍待完成。

- 2026-09-14 原生 macOS ARM64 应用包以独立应用名、独立用户目录连接隔离后端，实际完成顶部新建、问答编辑、自动保存及上下文预览，未调用模型。发现并修复远程客户端模式缺少 `i18n:changeLanguage` 本地 IPC 的问题；重新打包并启动后中文原生菜单正常，工作台可打开，正常退出且未再出现该 IPC 错误。此为应用目录包的客户端验收，不代表 DMG 安装、内嵌服务或真实模型验收。
- 最新整合回归：46 个文件、270 项测试、0 失败；语言启动回归另 4 项通过。全仓类型检查通过，语言修复后桌面类型检查与相关 lint 再次通过。主进程、preload、Pi 子进程、启用工作台的 renderer、资源校验及本地 ad-hoc 应用打包均通过；未公证或发布。

- 2026-09-14 最新隔离后端的实际界面保存链路：顶部新建草稿 `Recovery UI QA` → 执行 YAML 保存 → 会话编辑入口重新打开执行画布 → 修改目标并再次保存。任务始终为 `recovery-ui-qa`，工作台 `e4601b21-0dca-49d7-a3ce-4080d5402543` 的关联 ETag 与任务一致，任务目录仅有 `task.yaml`，无运行记录。修复此流程中保存阶段误显示 `Starting…` 的状态文案。此流程没有调用真实模型，也不代替崩溃恢复、审批或完整界面验收。

- 2026-09-14 合并回归继续覆盖工作台共享模块、RPC、重放、问答生命周期、Pi 输入/取消及流式请求关联、编排 UI 单测及 IPC 清单；不代表真实模型或完整发布验收。整个 server-core 会话模块额外执行 321 项测试、0 失败（33 个文件），覆盖新增输出订阅对现有会话的影响。
- 真实浏览器：统一标题在思考/执行视图间保留；执行来源面板空状态正常；修复两类历史组件 React key 冲突后，四次节点切换均只有一个生成历史入口。
- PDF 样本：两页文本、原页显示、第二页选区 0–1272 的持久化来源验证通过；修复选区节点缺少完整页列表时原页不显示的问题。150% 缩放实际重绘为 1147×1485，无错误；单页画布限制为 4096 单边、8M 像素，并清理取消/异常页面。
- DOCX 样本：发现并修复 raw-text 提取遗漏脚注。改为禁用外部文件访问的本地 HTML 转换，再以无 DOM 解析器提取文本；浏览器和真实 Mammoth 转换回归均保留正文、脚注标记及内容，原件字节不变。
- 生成回执已增加持久序号及工作区事件推送，客户端拒绝重复/乱序/跨文档回执，轮询用于恢复丢失事件。问答增量文本经 Pi 请求 ID/序号关联传递，按 250ms 节流持久化并单独展示为预览；尾部文本由定时器补刷。Agent 通过专属会话订阅区分追加的过程文本和替换的正式答案预览，过程折叠展示；正式答案到达后清除预览，取消后清理订阅与定时器并忽略增量。已覆盖重复、乱序、跨请求、结束后文本、尾部刷新、重复取消订阅及预览不进入答案版本的测试；真实模型流式与实际审批交互仍待验收。
- 生成回执保存历史执行模式，历史会话入口不再依赖节点当前模式；问答临时会话清理后不展示失效入口。
- 关联写入日志可恢复文档写入失败，拒绝跨越变更后的执行草稿；首次创建和已有任务保存均已在任务原子写入前记录精确目标 ETag，覆盖此前任务先写入的窗口。V1/V2 关联思考编辑不重写任务原文件、不生成迁移历史的 RPC 回归通过。

- 思考上下文、工作台存储与 RPC 单元测试。
- 执行图编辑及既有 TaskSpec 字段往返回归测试。
- Electron、server-core 类型检查；改动 UI 文件 ESLint。
- 国际化键一致性和排序；`git diff --check`。
- 开启开关的 renderer 生产构建通过（仍有大 chunk 警告）。这不等于桌面安装包或真实模型验收。

## 开发开关

在仓库根目录运行 `CRAFT_FEATURE_THOUGHT_WORKBENCH=1 bun run electron:dev`。
关闭开关回到旧入口，不删除 `thought-workbenches` 文档或已保存 TaskSpec。
不要在用户真实工作区用未验收功能执行 Agent 工具；先使用隔离测试工作区。
