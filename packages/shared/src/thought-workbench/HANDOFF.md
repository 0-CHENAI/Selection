# 编排工作台预提交交接

本分支是供继续开发、测试的检查点，不是已验收发布版本。基于 `test` 的 `097303e64c5ab0b73a378220fe3f07dd986ffd42`，默认功能开关仍关闭，不应直接合入发布。

## 启动

在隔离工作区使用 `CRAFT_FEATURE_THOUGHT_WORKBENCH=1 bun run electron:dev`。通过顶部“创建编排”进入思考视图；已有编排编辑进入执行视图。关闭开关保留旧入口，不删除新文档。模型连接在 Selection 内配置；没有加入或测试名为 Fable5 的模型配置，也未提交任何测试密钥。

## 当前证据

- 输入／存储链路 146 项回归通过；执行编辑、导入、提案和运行器 241 项回归通过。
- 新增全部节点类型字段保真回归，相关文件 37 项通过；断线发送清理回归通过。
- 本地确定性模型端点实际验证问答与 Agent：预览不发模型请求、正式生成、答案持久化、无自动 DAG 运行；问答预览后改变连接配置会阻止发送。
- 曾通过完整桌面构建、macOS ARM64 ad-hoc 应用目录包与签名／许可核对。之后新增问答快照和请求清理代码，旧应用包不是本检查点的完整产物，需重新构建。
- 更详细的测试与实现记录见 `IMPLEMENTATION.md`；其中早期“待完成”描述以较新的验证记录为准。

## 已知问题与剩余验收

1. **已复现 UI 问题**：800×700 窗口下，思考画布与编辑面板采用单列布局，画布最小高度占据剩余空间，`aside` 仅约 24px 高。位置：`ThoughtCanvas.tsx` 的响应式 grid / min-height。需修复窄屏滚动或布局分配，并实际验证，不只是修改静态断言。
2. 真实模型测试尚未完成，包括三轮局部提案、手动编辑失效、重新提案、保存运行、结果回流，以及 Agent 工具审批／取消／恢复与回答质量、Token 对比。模拟端点证据不可代替真实模型结论。
3. 完整历史迁移／冲突恢复 UI、材料内存与流式性能、安装包内嵌服务及其余产品验收仍须按原计划核对。不要将部分单元测试或桌面客户端测试扩写为全量通过。
4. 所有验收完成后再启用默认入口、清理旧入口、同步 #354/#320，并创建面向 `test` 的正式 PR。本次预提交不表示整个目标已完成。

## 数据安全边界

工作台草稿和提案应用不启动任务；运行仍以明确保存的 V3 TaskSpec 为准。保留 TaskRunner、冻结运行版本、审批及 revision 流程。请勿为测试而绕过审批、静默迁移旧任务或重跑中断的 Agent 工具。
