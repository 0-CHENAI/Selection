# v0.11.10 — HTML 链接按网页预览

## 修复

- 点击 `.html` / `.htm` 本地链接时，直接渲染页面，不再以源码高亮展示
- 预览使用沙箱 iframe；页头仍可「打开 / 在文件夹中显示」
- 相对路径的样式与图片会按该 HTML 文件所在目录解析

## 升级说明

请安装 **0.11.10**，在「关于」中确认版本后，再点开生成内容里的 HTML 链接验证。
