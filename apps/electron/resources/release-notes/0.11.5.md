# v0.11.5 — Selection 产品迭代

Selection 产品层能力与体验修复。

## 连接与欢迎页

- 欢迎连接方式改为 **ORDER** 优先（`https://order.ai.jxepdi.top`）
- Anthropic 走根路径（无 `/v1`），OpenAI 兼容走 `/v1`，均需 API 密钥
- 移除 Claude / ChatGPT / GitHub Copilot 默认入口

## 权限确认（询问修改）

- 对话中权限卡片按钮与提示中文化：允许 / 始终允许 / 不允许 等

## 工作区资源

- **Skill** 支持从其他本地工作区复制（与 MCP/Source 一致）
- 侧栏标题栏增加「从其他工作区复制」入口（Sources / Skills）
- 修复向 **中文路径工作区** 发送 Skills/MCP 时的路径与 UTF-8 编码问题
- 工作区 slug 支持中文文件夹名

## 其它

- 删除工作区时正确清理磁盘目录
- 默认权限模式：询问修改（ask）
