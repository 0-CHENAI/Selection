# v0.11.11 — 通知应用名与预览加固

## 修复

- 系统通知来源显示 **Selection**，不再显示 Electron App Selection
- HTML 预览不再通过 `file://` 解析相对路径，避免读到工作区以外的本地文件
- HTML 预览量不到尺寸时不再空白，页面仍会显示
- 文件链接后缀匹配仅在 Windows 上忽略大小写，避免 Linux 打开错文件

## 升级说明

请安装 **0.11.11**，在「关于」中确认版本。通知名需要重装后才会更新。
