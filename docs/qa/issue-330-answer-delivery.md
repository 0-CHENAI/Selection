# Issue #330 正文交付修复

## 三模型收尾验收（2026-09-10）

本轮按用户最新要求，以现有 ORDER 连接的 **Laufry、MO、Opus** 为真实模型验收范围。三者实际 SDK 记录均为 `openai-completions`，文本没有 provider phase 元数据；本次证明显式交付在这三种现有模型上成立，不宣称验证了未配置的独立 phase provider。明确 phase 的兼容路径继续由事件回归覆盖。

### 本轮发现与修复

1. **迟到的编排状态重新折叠正式正文。** 原归组逻辑在接受答案后，仍会因 `isManagedSwarmRunning` / `isTaskOrchestrationRunning` 将回合重开。新增回归先复现正式正文变为 `undefined`，随后改为以最新回合已接受的交付身份为准；旧答案不能替后续新回合完成收束。服务端对未完成 worker 和聚合合同的校验保持不变。
2. **Opus 将历史整篇答案附加到一句话追问。** 将正文交付和隐藏收口提示中的“完整”限定为当前用户请求，明确遵守所需范围和长度。修改后重新运行三模型的真实分支及重启续聊；三者都保留上下文并简短作答，Opus 不再重发历史整篇正文。
3. **查看器与 Web UI 检查阻挡。** 两个归组 helper 直接检查数组最后一项，保持原语义并消除 `findLast`/`at` 的库版本依赖；Web UI 补齐只用于静态导入、运行时明确抛错的 `realpathSync` shim。没有改变浏览器文件访问能力。
4. **补齐跨层验证。** 新增服务端事件 → renderer processor → 真实磁盘持久化 → 重载 → UI 归组集成测试，覆盖 `final`/`unclassified` 与正常提交/隐藏收口的四种组合，并重放正式完成事件验证幂等。

### 真实模型矩阵

下面每项均产生且只产生一条正式答案，回合停止后回读持久化内容和真实 SDK 工具结果锚点。短答均为 `4`；其余场景均保留完整解释、列表/表格或枚举结果，概率结论与实际工具结果一致。长文场景使用产品原有提示和工具，不手工注入 provider phase。先运行矩阵发现提示范围问题，再在修订提示后重跑三模型分支短答和重启续聊。

| 模型 | 场景 | 正文字符数 | 自动收口次数 | 正式消息 ID |
| --- | --- | ---: | ---: | --- |
| Laufry | 蒙提霍尔讲解及模拟 | 1421 | 0 | `msg-1789034821783-gvsr47` |
| Laufry | 纯文字短答 | 1 | 0 | `msg-1789034824814-n6z6i8` |
| Laufry | 过程说明 → 枚举 → 长答案 | 1492 | 0 | `msg-1789034849915-p393bb` |
| Laufry | 检验并纠正 1/2 假说 | 1264 | 0 | `msg-1789034967656-awwkq7` |
| MO | 蒙提霍尔讲解及模拟 | 1888 | 0 | `msg-1789034840834-d2w7v2` |
| MO | 纯文字短答 | 1 | 0 | `msg-1789034847230-myg7oj` |
| MO | 过程说明 → 枚举 → 长答案 | 1950 | 0 | `msg-1789034934067-xxqtrm` |
| MO | 检验并纠正 1/2 假说 | 3298 | 0 | `msg-1789035131142-fynzzm` |
| Opus | 蒙提霍尔讲解及模拟 | 2717 | 0 | `msg-1789034938247-77op5s` |
| Opus | 纯文字短答 | 1 | 1 | `msg-1789034953395-srz63m` |
| Opus | 过程说明 → 枚举 → 长答案 | 2977 | 0 | `msg-1789035148401-6hwdlx` |
| Opus | 检验并纠正 1/2 假说 | 1849 | 0 | `msg-1789035409765-26314a` |

- Laufry 的纠错场景首次遇到上游错误，没有伪造正式答案；同场景重新运行后通过。该重试是验收重试，不是无限自动收口。
- Opus 的纯文字短答真实触发一次隐藏收口，最终仍归入原回合。其他矩阵场景无需收口。
- 工具执行中出现过 uv 沙箱缓存权限失败和模型脚本错误；模型使用允许的本地工具纠正后获得计算结果。验收不声称所有工具调用均无错误。
- 本次核对了问题所需的概率结论和模拟/枚举结果。模型额外发挥的历史叙述、概率术语或表达严谨程度，不属于结构性交付保证；保留原始答案，不以人工改写掩盖模型错误。

### 原始输出顺序的额外真实回归

上述 12 个场景中的蒙提霍尔提示虽然要求“先完整讲解”，但原始事件表明模型先输出的是短过程说明，因此不能把那三次运行当作长讲解先于工具的证据。为避免用最终结果替代原始事件验收，又明确要求“普通可见文本完整讲解 → 本地模拟 → 一句补充 → 正式交付”，三模型各补跑一次，总计 **15 个场景**。

| 模型 | 首次工具前可见讲解字符数 | 正式正文字符数 | 正式答案 ID |
| --- | ---: | ---: | --- |
| Laufry | 1209 | 1666 | `msg-1789035977095-z6i675` |
| MO | 942 | 1150 | `msg-1789036006366-1urpp2` |
| Opus | 926 | 1282 | `msg-1789036112297-13m1pi` |

原始持久化消息确认这三段长讲解均在首次工具调用之前，包含主持人规则、编号推理和 Markdown 概率表；工具结束后确有短补充。三条正式答案均包含完整规则、推理、表格和真实模拟结果，只有一次接受交付，且无需自动收口。逐条回读 SDK 锚点指向实际提交工具结果。

这三条新答案再次经过浏览器实际按钮验收：Markdown、复制、重载均与正式正文逐字符一致，分支按钮 ID 正确；即使同时传入迟到的 Swarm 和任务编排运行标记，完整正文仍不折叠。Laufry 显示三张表，MO 与 Opus 各两张表。此项验证补齐了 #330 原始输出顺序的真实模型证据。

### 展示、复制与分支

三种模型的蒙提霍尔实际持久化记录均载入既有 TurnCard，并实际点击按钮：

| 模型 | 折叠工作链后正文 | 表格 | Markdown 与保存正文相等 | Copy 与既有纯文本转换相等 | 重载相等 | 分支 ID 相等 |
| --- | --- | ---: | --- | --- | --- | --- |
| Laufry | 完整 | 2 | 是 | 是 | 是 | 是 |
| MO | 完整 | 2（含 datatable） | 是 | 是 | 是 | 是 |
| Opus | 完整 | 2 | 是 | 是 | 是 | 是 |

同时用 Opus 的真实正文配合迟到的编排运行状态验证 UI，正式正文和两张表仍显示，Markdown 源文逐字符一致。截图为本地 `/tmp/issue330-final-opus.png`。

每个模型都实际从其正式答案 ID 创建分支，完成 SDK 分支准备后删除隔离工作区内的源会话，再进行一句话追问；随后销毁并重建 SessionManager，再追问上一条问答。三者均成功保留原答案及两条新交付，无回合级错误。上述删除只涉及验收创建的临时会话。

### 自动化与构建

最终相关回归：**39 个测试文件，547 项通过，0 失败**。包含 phase/intermediate/grouping、消息 ID 同步、交付入口/去重/收口、MCP/adapter、持久化/分支锚点及 Swarm 收束回归；新增跨层集成 4 项，迟到编排状态回归 2 项。

`typecheck:all`、Web UI 和 viewer 原始配置类型检查、仓库 lint、session MCP/Pi 子进程构建、服务端构建、viewer 构建及 Web UI 构建均通过。lint 与打包仅有既有 warnings；未使用修改类型库参数的替代检查。

验收原始数据、日志和临时预览不包含在生产提交中；日志检查使用身份、状态和错误分类，未额外记录完整正文。临时结果文件保存正文是为了进行内容一致性比较。

## 协议

普通用户会话使用 `explicit-v1`：模型通过 `submit_answer({ markdown })` 提交完整、独立的 Markdown。过程文本保留在工作链，provider 的 `final` 标记不再直接表示正式答案已经交付。

第一版不引用或拼接旧片段；验证推翻前文时，由模型提交修正后的完整答案。短回复、澄清问题和拒绝回答允许直接提交。结构校验不保证答案的语义完整性。

消息保存 `answerProtocol`、`answerRunId`、`answerCommitted`；用户消息另外保存 `answerRecoveryAttempted`。字段缺失的历史回合保留原有展示。隐藏的续跑沿用原任务身份，独立后台唤醒则建立新的交付回合。

## 执行与持久化

- session tool 注册表提供工具定义、参数校验和 Explore 模式许可。实际启用的 Pi backend 在主进程调用服务端交付入口；子进程只提供 SDK 身份和执行请求，不能自行宣告交付成功。
- 接受前校验活跃回合、重复/并发提交、前台工具、Swarm worker 与聚合合同。SDK 同批工具数不是 1 时禁止提交答案。
- 正文及分支锚点先保存，再回读确认消息确实落盘，随后发布正式正文事件。成功回执不重复完整正文，工作链不保存提交工具的原始 Markdown 参数。
- 成功提交后，执行检查阻止后续工具；SDK 保存工具结果后终止本轮，并把分支锚点更新到包含完整回执的日志条目。
- 自然完成但漏交付时，最多增加一次同模型、同上下文的隐藏收口。收口只暴露并允许 `submit_answer`，调用量计入原回合。
- 取消、失败、计划/认证交接、等待 Swarm worker 不触发收口。收口仍失败时保留工作内容并报告“未完成答案交付”。重启不自动调用模型。
- 内部 worker、Conductor 和非普通 system prompt 会话不启用此协议。

## 展示合同

正式卡片只使用已提交的正文，不用更长的流式草稿替换它。复制、Markdown 预览和弹出阅读复用这份正文。重复回执或迟到文本不会覆盖或降级正式答案；新交付回合不会被上一轮的去重规则隐藏。

## 验证记录（2026-09-10）

最终验证：23 个测试文件共 328 项通过；`typecheck:all`、仓库 lint（仅既有 warnings）、Pi 构建、服务端构建及 Web UI 类型检查通过。viewer 在命令行启用 ES2023 库后通过。

新增测试覆盖 SDK 批次限制、主进程桥接、服务端交付/收口、持久化和锚点、renderer 消息身份、正式正文归组及历史兼容；同时运行原有 phase/intermediate/grouping、空回复、Swarm 和用量回归。

实际使用本机配置的兼容 API 连接 `Laufry`，在临时工作区执行蒙提霍尔解释与 10,000 次模拟。模型成功提交 3401 字符正文，包含完整规则、编号讲解、概率表和模拟结果，无须自动收口。正式消息和 SDK 工具结果锚点均已回读确认。模拟中发生过脚本运行错误并由模型修正，最终正常交付。

将该会话的实际持久化数据载入现有 TurnCard 后，确认完整讲解及 3 张表格显示，折叠工作链后正文仍在。用户重新授权后完成实际按钮验收：Markdown 回调返回 3401 字符，与持久化正文逐字符一致；Copy 后通过原生粘贴读取 3099 字符，与现有纯文本转换结果逐字符一致。分支菜单回调绑定正式答案 ID `msg-1789018074288-sorryn`；重载后仍显示同一份正文及 3 张表格。分支后删除源会话、重建管理器再继续真实模型对话的验证见 `issue-322-325-session-independence.md`。

补充检查发现 ORDER 连接除默认 Laufry 外，还配置了 Opus 和 MO；此前只检查默认模型，遗漏了 MO。MO 的真实运行记录见下文。当前已验证的 Laufry 和 MO 都未提供明确 phase，带明确 phase 的 provider 仍仅完成事件回归，尚未真实模型验收。

当时独立 viewer 类型检查被共享组件的 `Array.findLast`/`Array.at` 阻挡，曾临时启用 ES2023 检查。下述收尾修改已消除这两处依赖，现可直接使用 viewer 原始配置检查，不再需要覆盖参数。

验收预览使用 renderer 下的 `issue330-preview.html`、`issue330-preview.tsx` 和 `issue330-preview-data.json`。临时预览未加入生产构建入口或提交。

## 提交前审查修复

- 阻止停止处理后的历史回退逻辑将新协议的未交付过程文本提升为正式答案；旧回合保持原行为。
- 在答案和收口状态的持久化等待之后重新校验取消及回合身份，防止取消后发布正文或发起收口；回滚仅恢复仍属于本次答案的最后回复元数据。
- 补齐 JSON Schema 工具列表的协议开关传递，确保调用方关闭答案工具时真正隐藏它。
- 忽略已交付回合的迟到文本增量和完成事件，同时保留后续回合正在生成的内容。
- 补齐无起始事件的工具结果及中断流式文字的持久化协议身份。

前三个生命周期缺陷有先失败、修复后通过的回归证据。审查修复后运行同一组 23 个测试文件；真实模型及浏览器记录沿用上述验收，本次未重新接管用户浏览器。


## MO 补充真实验收（2026-09-10）

通过真实 SessionManager 在隔离临时工作区运行，明确选择 ORDER 连接中的 `MO`，未修改用户默认模型或连接协议。实际 SDK 日志记录模型为 `MO`，协议为 `openai-completions`。四条有文本的 assistant SDK 消息均无 `textSignature`/phase；所有未交付文本事件为 `unclassified`。交付消息的 `final` 由服务端生成，不能作为 provider 提供明确 phase 的证据。

- 正式答案 ID：`msg-1789034447415-3dzluh`；回合 ID：`msg-1789034378939-icyp2s`。
- 持久化后只有一条 `answerCommitted` 消息，正文 1348 个字符，包含主持人规则、编号推理、理论概率表和模拟结果表；`answerRecoveryAttempted: false`，无回合级错误。
- 实际模拟 10,000 次，不换门胜出 3357 次（33.57%），换门胜出 6643 次（66.43%）。正文结果与 SDK 的 Read 工具结果一致。
- 首次 `script_sandbox` 因 uv 缓存文件被沙箱拒绝而失败；模型随后用 `transform_data` 成功计算并读取结果。该失败未被计作“无工具错误”。
- 分支锚点 `a919d300` 已回读，指向实际 `mcp__session__submit_answer` 的 SDK `toolResult`。
- 将本次实际持久化消息载入既有 TurnCard：工作链折叠时完整正文及两张表仍显示；Markdown 按钮返回的 1348 字符与持久化正文逐字符一致。Copy 经原生粘贴取得 1167 字符，与现有 `markdownToPlainText` 转换逐字符一致。
- 分支菜单回调绑定上述正式答案 ID；页面重新加载后 Markdown 仍逐字符一致、仍显示两张表。本次验证了分支绑定及 SDK 锚点；完整创建分支再续聊的已有验收记录见上文。

语义限制：模型在结尾添加了不准确的历史叙述，本次未修改模型原文来掩盖问题。概率结论和模拟数据有工具证据，正文交付及展示合同通过；这些证据不代表答案中所有额外事实均正确，也不填补明确 phase provider 的真实验收缺口。
